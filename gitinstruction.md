![logo](Git-Icon-Black.png)
# Работа с Git 

## 1. Проверка наличия установленного Git
В терминале выполнить комнаду `git --version`.
 Если Git установлен появится сообщение с информацией о версии программы, инчае будет сообшение об ошибке.

 ## 2. Установка Git 
 Загружаем последнюю версиб Git с [сайта](https://git-scm.com/download/win) .
 устанавливаем с настройками по умолчанию . 
 
 ## 3. Настройка Git
 Имя пользователя нужно, чтобы привязывать коммиты к вашему имени. Это не то же самое, что имя пользователя учётной записи GitHub, с помощью которого выполняется вход в профиль на GitHub. Задать или изменить имя пользователя можно с помощью команды git config. Новое имя будет автоматически отображаться в последующих коммитах, отправленных на GitHub через командную строку. Если хотите скрыть своё реальное имя, можно использовать в качестве имени пользователя Git произвольный набор символов
 При первом использовании Git необходимо представиться. Для этого нужно ввести в терминале 2 команды:
```
 git config --global user.name «Ваше имя английскими буквами» 
 giе config --global user.email ваша почта@example.com
```
Кроме того, командой `git config` можно изменять адрес электронной почты, привязанный к вашим коммитам Git. Новый адрес электронной почты будет автоматически отображаться во всех дальнейших коммитах, поданных на GitHub через командную строку.
## 4. Инициализация репазитория 
Если у вас уже есть проект в каталоге, который не находится под версионным контролем Git, то для начала нужно перейти в него. Если вы не делали этого раньше, то для разных операционных систем это выглядит по-разному:
для Windows:
 `cd C:/Users/user/my_project` (пример пути репазитория),
а затем выполните команду:
```
$ git init
```
Эта команда создаёт в текущем каталоге новый подкаталог с именем .git, содержащий все необходимые файлы репозитория — структуру Git репозитория. На этом этапе ваш проект ещё не находится под версионным контролем. Подробное описание файлов, содержащихся в только что созданном вами каталоге .git, приведено в главе Git изнутри
Если вы хотите добавить под версионный контроль существующие файлы (в отличие от пустого каталога), вам стоит добавить их в индекс и осуществить первый коммит изменений. Добиться этого вы сможете запустив команду git add несколько раз, указав индексируемые файлы, а затем выполнив `git commit`:
```
 git add <название репазитория>
```
## 5. Запись изменений в репозиторий
Команда `git add` добавляет изменение из рабочего каталога в раздел проиндексированных файлов. Однако на самом деле команда `git add` не оказывает существенного влияния на репозиторий: изменения регистрируются в нем только после выполнения команды `git commit`.
Наряду с этими командами вам понадобится команда `git status`, которая показывает состояние рабочего каталога и раздела проиндексированных файлов.
Для отмены коммита или проиндексированного снимка состояния используется команда `git reset`.

## 6. Просмотр историй коммитов 
для просмотра и использования в дальнейшем переходов по коммитам нужна знать их код, для этого есть команда:

 `git log` – вывод на экран истории всех коммитов с их хеш-кодами.  
 `git log --oneline` - выводит короткий список коммитов.

 Просматривать изменения, внесённые в репозиторий, можно с помощью параметра log. Он отображает список последних коммитов в порядке выполнения. Кроме того, добавив флаг -p, вы можете подробно изучить изменения, внесённые в каждый файл.
```
git log -p
```

## 7. Перемещение между сохранениями ( коммитами )
 Вместо полного кода достаточно испльзовать первые 4 символа, после чего для просмотра репазитория в момент определенного коммита пишем
 команду :
 ```
 git chekout <хеш-код коммита на который хотите перейти>
 ```
 для того что бы вернутся к последнему коммиту нужно использовать команду :
 ```
 git chekout master 
 ```
## 8. Игнорирование файлов
Для того, чтобы исключить отслеживание в репазитории определённые файлы или папки необходимо создать файл ***.gitignore*** и записать в него их названия или шаблоны соответствующие этим папкам или файлам (*png), (*jpg) и тд.

## 9. Создание веток в Git
Создать новую ветку можно с помощью параметра branch, указав имя ветки.
```
git branch <имя ветви>
```
Но Git не переключится на неё автоматически. Для автоматического перехода нужно добавить флаг -b и параметр checkout.
```
git checkout -b <имя ветви>
```

# 10. Слияние веток и разрешение конфликтов 

Команда слияния двух веток (примечение:перед тем как сливать ветки , новую ветку с веткой master, её надо сохранить и закоммитить ) :
```
git merge <имя ветви которую сливаем в основную>
```
Список веток в репозитории можно посмотреть с помощью комнады `git branch` . Текущая ветка будет отмечана звёздочкой: **\*master**
Ошибка может появится из-за разницы комитов при их слиянии.
комиит слияний нужно будет сделать после ошибки **обязательно** Перед этим воспользовавщись подсказской интерфейса программы в который вы используете git . Интерфейс укажет на разницу слияемой версии и той что находится на ветке **master** . Нужно будет выбрать версию которая нужна для слияния и оставить её . Это делается либо вручную, либо с помощью интерфейса программы . Обе версии будут выделенны  : >>> ; <<<.
# 11. Удаление веток
Удалить ветку можно параметром branch с добавлением флага -d и указанием имени ветки. Если вы завершили работу над веткой и объединили её с основной, можно её удалить без потери истории. Однако, если выполнить команду удаления до слияния — в результате появится сообщение об ошибке. Этот защитный механизм предотвращает потерю доступа к файлам.
```
git branch -d <имя удаляймой ветви>
```
Для принудительного удаления ветки используется флаг -D с заглавной буквой. В этом случае ветка будет удалена независимо от текущего статуса, без предупреждений.
```
git branch -D <имя удаляймой ветви>
```
Вышеуказанные команды удаляют только локальную копию ветки. В удалённом репозитории она может сохраниться. Если хотите стереть удалённую ветку, выполните следующую команду:
```
git push origin --delete <имя удаляймой ветви>
```

 # 12. Работа с удалёнными репазиториями

 1. Создать аккаунт на  GitHub
 2. Создать локальный репозиторий 
 3. Создать удалённый репозиторий 
 5. Связать удалённый репозиторий с локальным

 Добавить репозиторий к проекту :
```
git remote add <имя для репазитория> <url-адрес репазитория в сети>
```
Для получения и слияния изменений из удалённого репозитория используется команда: `git pull`

Отправить изменения локального репазитория в удалённый: `git push`

Список удалённых репазиториев `git remote`

Для работы с удалённым репазиторием другого пользователя его следует скапировать через Fork и добавить к себе в аккаунт GitHub. Далее 
через:

 ```
 git clone <скопированная ссылка с GitHub>
 ```
 После чего репазиторий будет добавлен к вам , но это нужно ( рекомендуется ) делать через корневой терминал. Потом после работы с данным файлом его можно отправить либо  через  
 ``` 
 git push --set-upstream origin new 
 ```


# Основные команды Git 

* ✦ git diff - выводит информацию об изменениях в файле с момента коммита , если они не сохранены .
* ✦ git init – инициализация локального репозитория
* ✦ git status – получить информацию от git о его текущем состоянии
* ✦ git add – добавить файл или файлы к следующему коммиту
* ✦ git commit -m “message” – создание коммита.
* ✦ git log --oneline - выводит короткий список коммитов.
* ✦ git log – вывод на экран истории всех коммитов с их хеш-кодами
* ✦ git checkout – переход от одного коммита к другому
* ✦ git checkout master – вернуться к актуальному состоянию и продолжить работу
* ✦ git diff – увидеть разницу между текущим файлом и закоммиченным файлом
* ✦ git comit --amend -m "" - позволяет измений комментарий последнего коммита